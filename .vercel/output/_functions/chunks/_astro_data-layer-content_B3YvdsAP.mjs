const _astro_dataLayerContent = [["Map",1,2,9,10],"meta::meta",["Map",3,4,5,6,7,8],"astro-version","5.17.1","content-config-digest","de3087ba3096a6d4","astro-config-digest","{\"root\":{},\"srcDir\":{},\"publicDir\":{},\"outDir\":{},\"cacheDir\":{},\"compressHTML\":true,\"base\":\"/\",\"trailingSlash\":\"ignore\",\"output\":\"server\",\"scopedStyleStrategy\":\"attribute\",\"build\":{\"format\":\"directory\",\"client\":{},\"server\":{},\"assets\":\"_astro\",\"serverEntry\":\"entry.mjs\",\"redirects\":false,\"inlineStylesheets\":\"auto\",\"concurrency\":1},\"server\":{\"open\":false,\"host\":false,\"port\":4321,\"streaming\":true,\"allowedHosts\":[]},\"redirects\":{},\"image\":{\"endpoint\":{\"route\":\"/_image\"},\"service\":{\"entrypoint\":\"astro/assets/services/sharp\",\"config\":{}},\"domains\":[],\"remotePatterns\":[],\"responsiveStyles\":false},\"devToolbar\":{\"enabled\":true},\"markdown\":{\"syntaxHighlight\":{\"type\":\"shiki\",\"excludeLangs\":[\"math\"]},\"shikiConfig\":{\"langs\":[],\"langAlias\":{},\"theme\":\"github-dark\",\"themes\":{},\"wrap\":false,\"transformers\":[]},\"remarkPlugins\":[],\"rehypePlugins\":[],\"remarkRehype\":{},\"gfm\":true,\"smartypants\":true},\"security\":{\"checkOrigin\":true,\"allowedDomains\":[]},\"env\":{\"schema\":{},\"validateSecrets\":false},\"experimental\":{\"clientPrerender\":false,\"contentIntellisense\":false,\"headingIdCompat\":false,\"preserveScriptOrder\":false,\"liveContentCollections\":false,\"csp\":false,\"staticImportMetaEnv\":false,\"chromeDevtoolsWorkspace\":false,\"failOnPrerenderConflict\":false,\"svgo\":false},\"legacy\":{\"collections\":false}}","docs",["Map",11,12,40,41,80,81],"welcome",{id:11,data:13,body:24,filePath:25,digest:26,rendered:27},{title:14,description:15,author:16,codeLocation:17,pubDate:18,categories:19},"Welcome to Internal Docs","Get started with the internal documentation site.","Internal Team","src/content/docs/",["Date","2025-02-11T00:00:00.000Z"],[20,21,22,23],"HFNY","Kinship","OCFS","PICHC","# Welcome\r\n\r\nThis is the **CHSR** documentation site. Docs are organized into four categories: **HFNY**, **Kinship**, **OCFS**, and **PICHC**. The Welcome doc appears in all categories.\r\n\r\nContent is stored as Markdown in **`src/content/docs/`**. That folder *is* the docs directory — add or edit `.md` files there; there is no separate directory list to update. After you push, the site rebuilds and new docs show up in the right category.\r\n\r\n- Add or edit `.md` files in `src/content/docs/` and push (or open a PR). The build picks up all files in that folder.\r\n- Each doc must have **frontmatter** with at least `title`, `author`, `pubDate`, and **`categories`** (one or more of HFNY, Kinship, OCFS, PICHC).\r\n- Use **`codeLocation`** in frontmatter so developers know where the related code lives (folder or repo path).\r\n\r\nFor step-by-step instructions, see **[How to add a doc](/docs/how-to-add-a-doc)**.","src/content/docs/welcome.md","6d8ead4ae0983232",{html:28,metadata:29},"<h1 id=\"welcome\">Welcome</h1>\n<p>This is the <strong>CHSR</strong> documentation site. Docs are organized into four categories: <strong>HFNY</strong>, <strong>Kinship</strong>, <strong>OCFS</strong>, and <strong>PICHC</strong>. The Welcome doc appears in all categories.</p>\n<p>Content is stored as Markdown in <strong><code>src/content/docs/</code></strong>. That folder <em>is</em> the docs directory — add or edit <code>.md</code> files there; there is no separate directory list to update. After you push, the site rebuilds and new docs show up in the right category.</p>\n<ul>\n<li>Add or edit <code>.md</code> files in <code>src/content/docs/</code> and push (or open a PR). The build picks up all files in that folder.</li>\n<li>Each doc must have <strong>frontmatter</strong> with at least <code>title</code>, <code>author</code>, <code>pubDate</code>, and <strong><code>categories</code></strong> (one or more of HFNY, Kinship, OCFS, PICHC).</li>\n<li>Use <strong><code>codeLocation</code></strong> in frontmatter so developers know where the related code lives (folder or repo path).</li>\n</ul>\n<p>For step-by-step instructions, see <strong><a href=\"/docs/how-to-add-a-doc\">How to add a doc</a></strong>.</p>",{headings:30,localImagePaths:34,remoteImagePaths:35,frontmatter:36,imagePaths:39},[31],{depth:32,slug:11,text:33},1,"Welcome",[],[],{title:14,description:15,author:16,codeLocation:17,pubDate:37,categories:38},["Date","2025-02-11T00:00:00.000Z"],[20,21,22,23],[],"how-to-add-a-doc",{id:40,data:42,body:47,filePath:48,digest:49,rendered:50},{title:43,description:44,author:16,codeLocation:17,pubDate:45,categories:46},"How to add a doc","Step-by-step guide to adding or updating documentation. Includes updating the docs directory and where developers keep code.",["Date","2025-02-11T00:00:00.000Z"],[20,21,22,23],"# How to add a doc\r\n\r\nThis guide explains how to add or update a doc so it appears on the CHSR docs site.\r\n\r\n## 1. Create or edit the file in the docs directory\r\n\r\n**The docs directory is:** `src/content/docs/`\r\n\r\n- **Adding a new doc:** Create a new `.md` file in `src/content/docs/` (e.g. `src/content/docs/my-feature.md`).\r\n- **Editing:** Open the existing `.md` file and edit it.\r\n\r\nThere is no separate “directory list” to update. The site builds from every `.md` file in `src/content/docs/`. Adding or renaming a file there is enough — the next build will pick it up.\r\n\r\n## 2. Add frontmatter at the top\r\n\r\nEvery doc must have **YAML frontmatter** between the first two `---` lines. Include at least:\r\n\r\n```yaml\r\n---\r\ntitle: Your Doc Title\r\nauthor: Your Name\r\npubDate: 2025-02-11\r\ncategories: [HFNY]\r\n---\r\n```\r\n\r\n**Required:**\r\n\r\n| Field         | Description |\r\n|---------------|-------------|\r\n| `title`       | Display title of the doc. |\r\n| `author`      | Your name (or team name). |\r\n| `pubDate`     | Date written (YYYY-MM-DD). |\r\n| `categories`  | One or more of: `HFNY`, `Kinship`, `OCFS`, `PICHC`. Controls which section(s) the doc appears in on the home page. Use multiple for docs that apply to all, e.g. `[HFNY, Kinship, OCFS, PICHC]`. |\r\n\r\n**Optional:**\r\n\r\n| Field          | Description |\r\n|----------------|-------------|\r\n| `description`  | Short summary; shown in doc lists. |\r\n| `codeLocation`| Where the related code lives (folder path or repo link). Developers use this to know where to write or find code. |\r\n\r\n## 3. Write the content\r\n\r\nBelow the closing `---`, write your content in Markdown (headings, lists, code blocks, tables, etc.).\r\n\r\n## 4. Where developers keep code\r\n\r\n- Use the **`codeLocation`** field in frontmatter to point to the repo path or folder where the feature/code lives (e.g. `HFNY/Admin/MyPage.aspx` or a GitHub path).\r\n- That way everyone knows where to find or add code for a given doc. Keep this updated when you move or add code.\r\n\r\n## 5. Deploy\r\n\r\n- **Local:** Run `npm run dev` to see the doc locally.\r\n- **Production:** Push your changes to the repo. After merge, the site (e.g. on Vercel) will rebuild and the new or updated doc will appear in the right category sections.\r\n\r\n## Summary\r\n\r\n1. Add or edit a `.md` file in **`src/content/docs/`** (that *is* the directory — no extra list to update).\r\n2. Set **frontmatter**: `title`, `author`, `pubDate`, and `categories` (one or more of HFNY, Kinship, OCFS, PICHC).\r\n3. Use **`codeLocation`** so developers know where the code lives.\r\n4. Push to the repo so the site rebuilds and the doc shows in the right category on the home page.","src/content/docs/how-to-add-a-doc.md","09041f5d5ecbc700",{html:51,metadata:52},"<h1 id=\"how-to-add-a-doc\">How to add a doc</h1>\n<p>This guide explains how to add or update a doc so it appears on the CHSR docs site.</p>\n<h2 id=\"1-create-or-edit-the-file-in-the-docs-directory\">1. Create or edit the file in the docs directory</h2>\n<p><strong>The docs directory is:</strong> <code>src/content/docs/</code></p>\n<ul>\n<li><strong>Adding a new doc:</strong> Create a new <code>.md</code> file in <code>src/content/docs/</code> (e.g. <code>src/content/docs/my-feature.md</code>).</li>\n<li><strong>Editing:</strong> Open the existing <code>.md</code> file and edit it.</li>\n</ul>\n<p>There is no separate “directory list” to update. The site builds from every <code>.md</code> file in <code>src/content/docs/</code>. Adding or renaming a file there is enough — the next build will pick it up.</p>\n<h2 id=\"2-add-frontmatter-at-the-top\">2. Add frontmatter at the top</h2>\n<p>Every doc must have <strong>YAML frontmatter</strong> between the first two <code>---</code> lines. Include at least:</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"yaml\"><code><span class=\"line\"><span style=\"color:#B392F0\">---</span></span>\n<span class=\"line\"><span style=\"color:#85E89D\">title</span><span style=\"color:#E1E4E8\">: </span><span style=\"color:#9ECBFF\">Your Doc Title</span></span>\n<span class=\"line\"><span style=\"color:#85E89D\">author</span><span style=\"color:#E1E4E8\">: </span><span style=\"color:#9ECBFF\">Your Name</span></span>\n<span class=\"line\"><span style=\"color:#85E89D\">pubDate</span><span style=\"color:#E1E4E8\">: </span><span style=\"color:#79B8FF\">2025-02-11</span></span>\n<span class=\"line\"><span style=\"color:#85E89D\">categories</span><span style=\"color:#E1E4E8\">: [</span><span style=\"color:#9ECBFF\">HFNY</span><span style=\"color:#E1E4E8\">]</span></span>\n<span class=\"line\"><span style=\"color:#B392F0\">---</span></span></code></pre>\n<p><strong>Required:</strong></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>title</code></td><td>Display title of the doc.</td></tr><tr><td><code>author</code></td><td>Your name (or team name).</td></tr><tr><td><code>pubDate</code></td><td>Date written (YYYY-MM-DD).</td></tr><tr><td><code>categories</code></td><td>One or more of: <code>HFNY</code>, <code>Kinship</code>, <code>OCFS</code>, <code>PICHC</code>. Controls which section(s) the doc appears in on the home page. Use multiple for docs that apply to all, e.g. <code>[HFNY, Kinship, OCFS, PICHC]</code>.</td></tr></tbody></table>\n<p><strong>Optional:</strong></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>description</code></td><td>Short summary; shown in doc lists.</td></tr><tr><td><code>codeLocation</code></td><td>Where the related code lives (folder path or repo link). Developers use this to know where to write or find code.</td></tr></tbody></table>\n<h2 id=\"3-write-the-content\">3. Write the content</h2>\n<p>Below the closing <code>---</code>, write your content in Markdown (headings, lists, code blocks, tables, etc.).</p>\n<h2 id=\"4-where-developers-keep-code\">4. Where developers keep code</h2>\n<ul>\n<li>Use the <strong><code>codeLocation</code></strong> field in frontmatter to point to the repo path or folder where the feature/code lives (e.g. <code>HFNY/Admin/MyPage.aspx</code> or a GitHub path).</li>\n<li>That way everyone knows where to find or add code for a given doc. Keep this updated when you move or add code.</li>\n</ul>\n<h2 id=\"5-deploy\">5. Deploy</h2>\n<ul>\n<li><strong>Local:</strong> Run <code>npm run dev</code> to see the doc locally.</li>\n<li><strong>Production:</strong> Push your changes to the repo. After merge, the site (e.g. on Vercel) will rebuild and the new or updated doc will appear in the right category sections.</li>\n</ul>\n<h2 id=\"summary\">Summary</h2>\n<ol>\n<li>Add or edit a <code>.md</code> file in <strong><code>src/content/docs/</code></strong> (that <em>is</em> the directory — no extra list to update).</li>\n<li>Set <strong>frontmatter</strong>: <code>title</code>, <code>author</code>, <code>pubDate</code>, and <code>categories</code> (one or more of HFNY, Kinship, OCFS, PICHC).</li>\n<li>Use <strong><code>codeLocation</code></strong> so developers know where the code lives.</li>\n<li>Push to the repo so the site rebuilds and the doc shows in the right category on the home page.</li>\n</ol>",{headings:53,localImagePaths:74,remoteImagePaths:75,frontmatter:76,imagePaths:79},[54,55,59,62,65,68,71],{depth:32,slug:40,text:43},{depth:56,slug:57,text:58},2,"1-create-or-edit-the-file-in-the-docs-directory","1. Create or edit the file in the docs directory",{depth:56,slug:60,text:61},"2-add-frontmatter-at-the-top","2. Add frontmatter at the top",{depth:56,slug:63,text:64},"3-write-the-content","3. Write the content",{depth:56,slug:66,text:67},"4-where-developers-keep-code","4. Where developers keep code",{depth:56,slug:69,text:70},"5-deploy","5. Deploy",{depth:56,slug:72,text:73},"summary","Summary",[],[],{title:43,author:16,pubDate:77,description:44,codeLocation:17,categories:78},["Date","2025-02-11T00:00:00.000Z"],[20,21,22,23],[],"form-development-sop",{id:80,data:82,body:89,filePath:90,digest:91,rendered:92},{title:83,description:84,author:85,codeLocation:86,pubDate:87,categories:88},"Form Development SOP for New Developers","Standard operating procedure for building forms in the HFNY app — UI patterns, design, data access, testing, step-by-step form creation, JavaScript, landing pages, and database setup.","Ishan Pathak","HFNY/Admin/DeletedDataDashboard.aspx",["Date","2025-02-11T00:00:00.000Z"],[20],"# Form Development SOP for New Developers\r\n\r\n## Goal\r\n\r\nDocument the standard operating procedure for building forms in the HFNY app so new and future developers follow consistent patterns. The reference implementation is **HFNY/Admin/DeletedDataDashboard.aspx**.\r\n\r\n---\r\n\r\n## 1. Page structure and master page\r\n\r\n- **Master page**: All forms use `MasterPageFile=\"~/MasterPages/Site.Master\"`.\r\n- **Content placeholders** (in order):\r\n  - `HeadContent` – page-specific CSS/scripts in `<head>`\r\n  - `FormName` – optional; page title shown in the master's header (styled by `.form-title` in Content/style2.css: center, 16px, bold, margin 10px)\r\n  - `ContentPlaceHolder1` – main body\r\n  - `ScriptContent` – page-specific JavaScript (e.g. DataTable init, modal handlers)\r\n- **Body wrapper**: Wrap main content in `<div class=\"container\">` (or rely on master's `container-fluid` for full width).\r\n\r\n---\r\n\r\n## 2. Form panel layout (standard \"form\" look)\r\n\r\nPattern used in DeletedDataDashboard and other Admin/Pages:\r\n\r\n```html\r\n<div class=\"panel panel-primary form-panel\">\r\n    <div class=\"panel-heading\">\r\n        <span class=\"panel-title\">[Page Title]</span>\r\n        <div class=\"clearfix\"></div>\r\n    </div>\r\n    <div class=\"panel-body\">\r\n        <!-- filters, grid, form fields -->\r\n    </div>\r\n    <div class=\"panel-footer\">\r\n        <!-- Cancel left; Submit/primary right -->\r\n    </div>\r\n</div>\r\n```\r\n\r\n- **Panel classes**:\r\n  - `panel panel-primary` – main container (Bootstrap primary border/header).\r\n  - `form-panel` – custom background `#f2f2f2` (Content/customCSSClasses.css).\r\n- **Optional inner panels**: Use `panel panel-info` for filter/context blocks, or nested `panel panel-primary` for the data area (e.g. grid).\r\n- **Custom panel variants** (customCSSClasses): `custom-panel-success`, `custom-panel-warning`, `custom-panel-danger`, `custom-panel-secondary` for different header/state colors.\r\n\r\n---\r\n\r\n## 3. Buttons – CSS classes and placement\r\n\r\n**Standard classes (Bootstrap + project convention):**\r\n\r\n| Purpose                 | CSS class                     | Notes                                                                               |\r\n| ----------------------- | ----------------------------- | ----------------------------------------------------------------------------------- |\r\n| Cancel / back           | `btn btn-default`             | First in footer; use `<a href=\"...\">` or LinkButton with `CausesValidation=\"false\"`  |\r\n| Submit / primary action | `btn btn-primary`             | Save, Restore, Confirm, etc.                                                        |\r\n| Delete / destructive    | `btn btn-danger`              | Use for delete actions                                                              |\r\n| Secondary / info        | `btn btn-info`                | e.g. \"Reinstate\", \"Unlock\"                                                          |\r\n| Small (grid/modal)      | `btn btn-sm`                  | e.g. `btn btn-default btn-sm`                                                       |\r\n| Header actions         | `btn btn-default pull-right`   | Add, Edit, Print in panel-heading                                                   |\r\n\r\n**Icons:** Prefer Glyphicons with `&nbsp;` before text, e.g.:\r\n\r\n- Cancel: `<span class=\"glyphicon glyphicon-remove\"></span>&nbsp;Cancel`\r\n- Save/Submit: `<span class=\"glyphicon glyphicon-save\"></span>&nbsp;Save` or `glyphicon-floppy-disk`\r\n- Restore: `<span class=\"glyphicon glyphicon-repeat\"></span>&nbsp;Restore`\r\n\r\n**Footer order:** Cancel on the left, Submit (or primary action) on the right. Submit can use `pull-right` when both are in the same footer. Example (DeletedDataDashboard):\r\n\r\n```html\r\n<div class=\"panel-footer\">\r\n    <a href=\"AdministrationHome.aspx\" class=\"btn btn-default\">\r\n        <span class=\"glyphicon glyphicon-remove\"></span>&nbsp;Cancel\r\n    </a>\r\n</div>\r\n```\r\n\r\nFor forms with both Cancel and Submit (e.g. Admin/AddPIExemption.aspx, Admin/ReportCatalogDetails.aspx): Cancel first, Submit second; Submit often `btn btn-primary` with `pull-right` or placed after Cancel.\r\n\r\n---\r\n\r\n## 4. Toast notifications (postback feedback)\r\n\r\n- **Mechanism**: Session-based. Code-behind adds a `Notification` to session; master page's `PreRender` calls `NotificationsSys.DisplayNotifications()`; Site.Master JavaScript reads hidden fields and calls `showNotification(type, title, text, duration)` (jQuery toast plugin).\r\n- **Code-behind pattern** (BusinessObjects/Notification.cs, Modules/Utilities.cs constants):\r\n\r\n```csharp\r\nNotificationsSystem.SendNotificationToSession(new Notification\r\n{\r\n    NotificationTitle = \"Form Restored\",\r\n    NotificationType = Utilities.NotificationSuccess,  // or NotificationError, NotificationWarning, NotificationInfo\r\n    NotificationText = \"Form restored successfully!\",\r\n    NotificationDuration = 5000\r\n});\r\n```\r\n\r\n- **Types and colors** (from Site.Master `showNotification()`):\r\n  - **success**: `#5cb85c`\r\n  - **error**: `#d9534f`\r\n  - **warning**: `#f0ad4e`\r\n  - **info**: `#5bc0de`\r\n- **Durations**: 5000 ms typical; 15000 for long error messages.\r\n- Do not invent new type strings; use `Utilities.NotificationSuccess`, `Utilities.NotificationError`, `Utilities.NotificationWarning`, `Utilities.NotificationInfo`.\r\n\r\n---\r\n\r\n## 5. Inline alerts (static or server-toggled messages)\r\n\r\nFor messages that are part of the page content (not toasts):\r\n\r\n- **Classes**: `alert alert-success`, `alert alert-info`, `alert alert-warning`, `alert alert-danger`.\r\n- **Thin variant**: `alert-thin` (customCSSClasses.css) for compact single-line messages.\r\n- **Role**: Add `role=\"alert\"` for accessibility where appropriate.\r\n- **Usage**: EmptyDataTemplate, filter info, validation/error blocks (e.g. DeletedDataDashboard EmptyDataTemplate, TrainingMaterials, WorkerForm).\r\n\r\n---\r\n\r\n## 6. Data tables (GridView + DataTables)\r\n\r\n- **GridView**: `CssClass=\"table data-display\"` and `GridLines=\"None\"`. Optional: `table-hover`, `table-striped`, `table-condensed` for non–DataTable styling when not initializing with jQuery.\r\n- **DataTable initialization** (in `ScriptContent`): Target `.data-display`; check grid is visible and has rows; prepend `<thead>` from first row if needed; then call `.DataTable({ ... })`. Example options used in the project:\r\n  - `pageLength: 10`, `lengthMenu: [10, 25, 50, 100]`\r\n  - `stateSave: true`, `stateDuration: 60`\r\n  - `responsive: { details: { type: 'column' } }`\r\n  - `language.emptyTable`, `language.searchPlaceholder`\r\n  - `columnDefs` for non-sortable action column and responsive priority\r\n- **UpdatePanel**: Re-run the DataTable init in `Sys.WebForms.PageRequestManager.getInstance().add_endRequest(...)` so it works after partial postbacks.\r\n- **Empty state**: Use `<EmptyDataTemplate>` with `<div class=\"alert alert-info\">` and clear copy (e.g. \"No deleted forms found. Select a form type from the dropdown above.\").\r\n\r\n---\r\n\r\n## 7. Modals\r\n\r\n- **Structure**: `modal fade` → `modal-dialog` → `modal-content` → `modal-header` (with `modal-title` and close button) → `modal-body` → `modal-footer`.\r\n- **Buttons**:\r\n  - Close/Cancel: `class=\"btn btn-default\" data-dismiss=\"modal\"` (and optionally glyphicon-remove).\r\n  - Confirm/Primary: `class=\"btn btn-primary\"` (e.g. `asp:LinkButton` for postback or `button` for client-only).\r\n- **Close button in header**: `<button type=\"button\" class=\"close\" data-dismiss=\"modal\" aria-label=\"Close\"><span aria-hidden=\"true\">&times;</span></button>`.\r\n\r\n---\r\n\r\n## 8. Colors and utility classes (reference)\r\n\r\n- **Toast/notification colors**: See section 4 (success/error/warning/info hex values).\r\n- **Custom panel/table** (customCSSClasses): `custom-panel-success` (#28a745), `custom-panel-warning` (#e3ae0e), `custom-panel-danger` (#dc3545); `td-warning` (#e3ae0e), `td-danger` (#c42f3e); `text-danger` (#dc3545); `.green` / `.green-text`, `.red` / `.red-text`.\r\n- **Spacing**: `margin-top-10`, `margin-bottom-10`, `margin-right-5`, `margin-left-5`, `padding-5`, etc. (customCSSClasses).\r\n- **Form controls**: Use `form-control` for inputs/dropdowns; `form-group` for label + control grouping.\r\n\r\n---\r\n\r\n## 9. Common practices summary\r\n\r\n- **Cancel**: Always provide a way to leave without submitting (link back to parent or Cancel button). Use `btn btn-default`, left side of footer.\r\n- **Submit**: Primary action with `btn btn-primary`, often with ValidationGroup and OnClick. Place after Cancel; optional `pull-right`.\r\n- **Validation**: Use `ValidationGroup` on submit buttons and validators; set `CausesValidation=\"false\"` on Cancel.\r\n- **Hidden fields**: Use `<asp:HiddenField>` for IDs/keys used by JavaScript and postback (e.g. delete PK, form type).\r\n- **Delete in grid**: Use pattern `data-pk`, `data-hf` (hidden field ID suffix), `data-target` (modal selector); master page already wires `.delete-gridview` to set hidden field and show modal.\r\n- **Confirmation for destructive actions**: Use a modal with clear message; Cancel = `btn btn-default` dismiss; Confirm = `btn btn-primary` or `btn btn-danger` as appropriate.\r\n\r\n---\r\n\r\n## 10. Design (how we design)\r\n\r\n- **Requirements**: Clarify with stakeholders what the form must do (fields, filters, actions, who can see it). Decide if it is a new form, a list/dashboard, or an add/edit detail page.\r\n- **Reference existing pages**: Use **DeletedDataDashboard.aspx** for list + filters + grid + modal confirm; use **AdministrationHome.aspx** for landing pages; use **AddPIExemption.aspx** or **ReportCatalogDetails.aspx** for form-with-Submit/Cancel.\r\n- **Wireframes/sketches** (optional): For complex flows, sketch layout (filters, table, buttons) so markup and placeholders match.\r\n- **Consistency**: Reuse the panel structure, button classes, and patterns in this SOP so the new form looks and behaves like the rest of the app.\r\n\r\n---\r\n\r\n## 11. How we get data\r\n\r\nThe app uses two main data patterns (see TechincalDocumentation.md §7):\r\n\r\n- **Entity Framework (newer forms)**\r\n  - **Location**: HFNY/Model/EntityFramework/ — `HealthyFamiliesContext`, entities (e.g. `HVLogDeleted`, `Worker`), and HealthyFamiliesModel.edmx.\r\n  - **Usage**: In code-behind, create a `Model.EntityFramework.HealthyFamiliesContext` (or use existing pattern), write LINQ queries, then either bind entities to controls or build a `DataTable` and bind to a GridView.\r\n  - **Example**: DeletedDataDashboard.aspx.cs — `GetDeletedHVLogs()`, `GetDeletedSupervisions()`, etc. use `HealthyFamiliesContext` and LINQ, then fill a `DataTable` and set `gvDeletedForms.DataSource = dt; gvDeletedForms.DataBind();`.\r\n\r\n- **Enterprise Library + stored procedures (legacy)**\r\n  - **Location**: HFNY/BusinessObjects/ — classes call `GetStoredProcCommand(\"spName\")` and execute via Enterprise Library. Data often maps to BusinessEntities (e.g. `beWorker`).\r\n  - **Usage**: When a feature already has a BusinessObject and stored proc, call that from code-behind and bind the returned data to the UI.\r\n\r\n- **Typical flow in code-behind**\r\n  - **Page_Load**: If `!IsPostBack`, call a method that loads data (e.g. `LoadAllDeletedForms()`) and binds to GridView/DropDownList.\r\n  - **Events**: Dropdown `SelectedIndexChanged`, button `Click`, etc. call server methods that (optionally) reload data and rebind, then show a toast via `NotificationsSystem.SendNotificationToSession(...)`.\r\n\r\n---\r\n\r\n## 12. How we test\r\n\r\n- **Manual testing**: Primary approach. Run the app (e.g. F5 or deploy to test env), log in with a role that has access, exercise the form (load, filter, submit, cancel, validation, toasts). Test with different roles if the page is role-restricted.\r\n- **Unit tests**: The main project references `Microsoft.VisualStudio.QualityTools.UnitTestFramework` and has a Testing folder; automated unit tests are not consistently present. New developers can add unit tests for BusinessObjects or utilities if the team adopts that.\r\n- **Checklist for a new form**: Can the page be opened? Do filters/grid load? Do Cancel and Submit behave correctly? Do toasts appear after success/error? Does the DataTable (if used) sort/search and re-init after postback? Are validators and ValidationGroup correct?\r\n\r\n---\r\n\r\n## 13. Step-by-step: creating a new form\r\n\r\n1. **Create the page files**\r\n   - Add `YourPage.aspx` under the right folder (e.g. `HFNY/Admin/` or `HFNY/Pages/`).\r\n   - Set `MasterPageFile=\"~/MasterPages/Site.Master\"` and fill the four content placeholders (HeadContent, FormName, ContentPlaceHolder1, ScriptContent).\r\n   - Add `YourPage.aspx.cs` (and optionally `YourPage.aspx.designer.cs`). Inherit from `System.Web.UI.Page` and use the same namespace as the folder (e.g. `HFNY.Admin`).\r\n\r\n2. **Markup (ContentPlaceHolder1)**\r\n   - Wrap content in `<div class=\"container\">`.\r\n   - Use the standard panel: `panel panel-primary form-panel`, with `panel-heading` (title), `panel-body` (filters + grid or form fields), and `panel-footer` (Cancel, then Submit if needed).\r\n   - Add controls (GridView with `CssClass=\"table data-display\"` if using DataTables, DropDownList with `CssClass=\"form-control\"`, HiddenFields for keys, etc.).\r\n   - If you need a confirmation modal, add a `modal fade` block (see §7).\r\n\r\n3. **Code-behind**\r\n   - In `Page_Load`, if `!IsPostBack`, call a method to load data and bind (e.g. to GridView or DropDownList).\r\n   - Implement event handlers (e.g. `ddlFormType_SelectedIndexChanged`, `btnConfirmRestore_Click`). Load/save using EF or BusinessObjects, then rebind and/or `SendNotificationToSession`.\r\n   - Use `Utilities.NotificationSuccess` / `NotificationError` etc. and consistent durations (e.g. 5000 ms).\r\n\r\n4. **JavaScript (ScriptContent)**\r\n   - If the page has a DataTable, add a function that initializes `.data-display` (see §6) and call it on `jQuery(document).ready`. If the page uses UpdatePanel, register the same function in `Sys.WebForms.PageRequestManager.getInstance().add_endRequest(...)`.\r\n   - Add any modal or button handlers (e.g. set hidden fields, show modal) in the same script block.\r\n\r\n5. **Add to navigation**\r\n   - **Landing page**: If the form is under Settings/Admin, add a link on AdministrationHome.aspx in the right section (e.g. Administration) and RoleGroup: `<a href=\"../Admin/YourPage.aspx\" data-content=\"Description\">Your Page Title</a>`.\r\n   - **Top-level menu**: If it should appear in the main nav, add a node in Web.sitemap with the correct `url` and `roles`.\r\n\r\n6. **Authorization**\r\n   - Ensure the page is only reachable by allowed roles (same as other Admin pages, or add `<location>` in Web.config if needed). AdministrationHome uses `asp:LoginView` and RoleGroups to show/hide sections.\r\n\r\n7. **Test**\r\n   - Follow §12: manual test load, filters, submit, cancel, toasts, and (if applicable) DataTable behavior after postback.\r\n\r\n---\r\n\r\n## 14. Where and how we write JavaScript\r\n\r\n- **Page-specific inline (most common)**\r\n  - Put script in the **ScriptContent** placeholder of the .aspx page. The master page renders this after the bundled scripts (jQuery, Bootstrap, DataTables, etc.).\r\n  - Use it for: DataTable initialization, modal open/close, setting HiddenFields from grid buttons, and other logic that only that page needs.\r\n  - Example: DeletedDataDashboard.aspx — `ScriptContent` contains `initializeDeletedDataDashboardTable()`, click handler for `.btnRestore`, and `document.ready` (plus `PageRequestManager.add_endRequest` for postback).\r\n\r\n- **Shared / site-wide**\r\n  - Scripts used on many pages are in HFNY/Scripts/ and included via App_Start/BundleConfig.cs. The master page loads `~/bundles/SiteSpecific` (jquery.toast, bootbox, cleave, etc.).\r\n  - Global helpers (e.g. `showNotification`) are defined in Site.Master script block.\r\n\r\n- **Page-specific .js file**\r\n  - For large or reusable page logic, add a new .js file under `Scripts/` (e.g. `YourFeature.js`), register a new bundle in BundleConfig (e.g. `bundles/YourFeaturePage`), and reference that bundle only on the page that needs it (e.g. via ScriptManager or an extra script reference in HeadContent). Many pages instead keep everything in ScriptContent for simplicity.\r\n\r\n- **UpdatePanel / partial postback**\r\n  - After a partial postback, the DOM is replaced; any jQuery plugins (e.g. DataTables) must be re-initialized. Use `Sys.WebForms.PageRequestManager.getInstance().add_endRequest(function () { ... })` and call the same init function (e.g. `initializeDeletedDataDashboardTable()`).\r\n  - Use event delegation (e.g. `jQuery(\".data-display\").on(\"click\", \".btnRestore\", ...)`) so that dynamically added rows still work after rebind.\r\n\r\n---\r\n\r\n## 15. How to create a landing page\r\n\r\n- **Pattern**: Use **AdministrationHome.aspx** as the template. A landing page is a single page with one or more **sections** (e.g. \"Administration\", \"Program Management\", \"Self-Service\"). Each section is a `panel panel-primary` with `panel-heading` (title) and `panel-body` containing **links** to child pages.\r\n- **Markup**:\r\n  - `<div class=\"panel panel-primary\">`\r\n  - `<div class=\"panel-heading\"><h4 class=\"panel-title\">Section Name</h4></div>`\r\n  - `<div class=\"panel-body\">`\r\n  - Multiple `<a href=\".../ChildPage.aspx\" data-content=\"Tooltip\">Display Text</a>`\r\n  - Optional: add `list-group-item` class to links (AdministrationHome does this via jQuery).\r\n- **Role-based visibility**: Use `<asp:LoginView>` and `<RoleGroups>` so only certain roles see certain sections (e.g. Admin sees \"User Administration\", \"Restore Deleted Forms\", etc.). Add new links inside the appropriate RoleGroup.\r\n- **Adding a new child link**: Find the right panel and RoleGroup on AdministrationHome (or your landing page), then add a line: `<a href=\"../Admin/YourNewPage.aspx\" data-content=\"Short description\">Your New Page</a>`. No Web.sitemap change needed unless the page should also appear in the top-level menu.\r\n\r\n---\r\n\r\n## 16. Database setup (SSMS and frontend use)\r\n\r\nWhen a new form needs **new or changed database objects**:\r\n\r\n- **Connection configuration**\r\n  - Connection strings live in **connections.config** (or environment-specific config). The default database is typically named **HFNY** (see TechincalDocumentation §4–5). Do not commit real connection strings; use config transforms or externalized config per environment.\r\n\r\n- **Option A: New or changed tables (Entity Framework)**\r\n  - Create or alter tables in **SQL Server Management Studio (SSMS)** (or run a .sql script in the project/repo if the team uses one).\r\n  - In Visual Studio, update the **EF model**: open HFNY/Model/EntityFramework/HealthyFamiliesModel.edmx, right-click → \"Update Model from Database\", and add/refresh the tables. This regenerates entity classes under Model/EntityFramework.\r\n  - In code-behind (or a BusinessObject), use `HealthyFamiliesContext` and the new entities/DbSets to query and persist. No stored proc required for simple CRUD if using EF.\r\n\r\n- **Option B: Stored procedures (Enterprise Library)**\r\n  - Create or alter stored procedures in SSMS (or via a .sql script).\r\n  - In BusinessObjects (or rarely in code-behind), call `db.GetStoredProcCommand(\"YourSpName\")`, add parameters, execute, and map results to entities or a DataTable.\r\n  - Use this when the feature already follows the BusinessObject + stored-proc pattern or when complex logic is kept in the database.\r\n\r\n- **Summary for new developers**\r\n  - **New table, simple CRUD**: Create table in SSMS → Update EF model from database → use `HealthyFamiliesContext` and LINQ in the new page.\r\n  - **New stored proc**: Create proc in SSMS → call it from a BusinessObject or code-behind via Enterprise Library.\r\n  - **Existing tables**: Use existing EF entities or BusinessObjects; no SSMS change unless you add columns or procs.","src/content/docs/form-development-sop.md","6478890cdd6d7681",{html:93,metadata:94},"<h1 id=\"form-development-sop-for-new-developers\">Form Development SOP for New Developers</h1>\n<h2 id=\"goal\">Goal</h2>\n<p>Document the standard operating procedure for building forms in the HFNY app so new and future developers follow consistent patterns. The reference implementation is <strong>HFNY/Admin/DeletedDataDashboard.aspx</strong>.</p>\n<hr>\n<h2 id=\"1-page-structure-and-master-page\">1. Page structure and master page</h2>\n<ul>\n<li><strong>Master page</strong>: All forms use <code>MasterPageFile=\"~/MasterPages/Site.Master\"</code>.</li>\n<li><strong>Content placeholders</strong> (in order):\n<ul>\n<li><code>HeadContent</code> – page-specific CSS/scripts in <code>&#x3C;head></code></li>\n<li><code>FormName</code> – optional; page title shown in the master’s header (styled by <code>.form-title</code> in Content/style2.css: center, 16px, bold, margin 10px)</li>\n<li><code>ContentPlaceHolder1</code> – main body</li>\n<li><code>ScriptContent</code> – page-specific JavaScript (e.g. DataTable init, modal handlers)</li>\n</ul>\n</li>\n<li><strong>Body wrapper</strong>: Wrap main content in <code>&#x3C;div class=\"container\"></code> (or rely on master’s <code>container-fluid</code> for full width).</li>\n</ul>\n<hr>\n<h2 id=\"2-form-panel-layout-standard-form-look\">2. Form panel layout (standard “form” look)</h2>\n<p>Pattern used in DeletedDataDashboard and other Admin/Pages:</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"html\"><code><span class=\"line\"><span style=\"color:#E1E4E8\">&#x3C;</span><span style=\"color:#85E89D\">div</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"panel panel-primary form-panel\"</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;</span><span style=\"color:#85E89D\">div</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"panel-heading\"</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">        &#x3C;</span><span style=\"color:#85E89D\">span</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"panel-title\"</span><span style=\"color:#E1E4E8\">>[Page Title]&#x3C;/</span><span style=\"color:#85E89D\">span</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">        &#x3C;</span><span style=\"color:#85E89D\">div</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"clearfix\"</span><span style=\"color:#E1E4E8\">>&#x3C;/</span><span style=\"color:#85E89D\">div</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;/</span><span style=\"color:#85E89D\">div</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;</span><span style=\"color:#85E89D\">div</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"panel-body\"</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">        &#x3C;!-- filters, grid, form fields --></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;/</span><span style=\"color:#85E89D\">div</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;</span><span style=\"color:#85E89D\">div</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"panel-footer\"</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#6A737D\">        &#x3C;!-- Cancel left; Submit/primary right --></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;/</span><span style=\"color:#85E89D\">div</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">&#x3C;/</span><span style=\"color:#85E89D\">div</span><span style=\"color:#E1E4E8\">></span></span></code></pre>\n<ul>\n<li><strong>Panel classes</strong>:\n<ul>\n<li><code>panel panel-primary</code> – main container (Bootstrap primary border/header).</li>\n<li><code>form-panel</code> – custom background <code>#f2f2f2</code> (Content/customCSSClasses.css).</li>\n</ul>\n</li>\n<li><strong>Optional inner panels</strong>: Use <code>panel panel-info</code> for filter/context blocks, or nested <code>panel panel-primary</code> for the data area (e.g. grid).</li>\n<li><strong>Custom panel variants</strong> (customCSSClasses): <code>custom-panel-success</code>, <code>custom-panel-warning</code>, <code>custom-panel-danger</code>, <code>custom-panel-secondary</code> for different header/state colors.</li>\n</ul>\n<hr>\n<h2 id=\"3-buttons--css-classes-and-placement\">3. Buttons – CSS classes and placement</h2>\n<p><strong>Standard classes (Bootstrap + project convention):</strong></p>\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n<table><thead><tr><th>Purpose</th><th>CSS class</th><th>Notes</th></tr></thead><tbody><tr><td>Cancel / back</td><td><code>btn btn-default</code></td><td>First in footer; use <code>&#x3C;a href=\"...\"></code> or LinkButton with <code>CausesValidation=\"false\"</code></td></tr><tr><td>Submit / primary action</td><td><code>btn btn-primary</code></td><td>Save, Restore, Confirm, etc.</td></tr><tr><td>Delete / destructive</td><td><code>btn btn-danger</code></td><td>Use for delete actions</td></tr><tr><td>Secondary / info</td><td><code>btn btn-info</code></td><td>e.g. “Reinstate”, “Unlock”</td></tr><tr><td>Small (grid/modal)</td><td><code>btn btn-sm</code></td><td>e.g. <code>btn btn-default btn-sm</code></td></tr><tr><td>Header actions</td><td><code>btn btn-default pull-right</code></td><td>Add, Edit, Print in panel-heading</td></tr></tbody></table>\n<p><strong>Icons:</strong> Prefer Glyphicons with <code>&#x26;nbsp;</code> before text, e.g.:</p>\n<ul>\n<li>Cancel: <code>&#x3C;span class=\"glyphicon glyphicon-remove\">&#x3C;/span>&#x26;nbsp;Cancel</code></li>\n<li>Save/Submit: <code>&#x3C;span class=\"glyphicon glyphicon-save\">&#x3C;/span>&#x26;nbsp;Save</code> or <code>glyphicon-floppy-disk</code></li>\n<li>Restore: <code>&#x3C;span class=\"glyphicon glyphicon-repeat\">&#x3C;/span>&#x26;nbsp;Restore</code></li>\n</ul>\n<p><strong>Footer order:</strong> Cancel on the left, Submit (or primary action) on the right. Submit can use <code>pull-right</code> when both are in the same footer. Example (DeletedDataDashboard):</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"html\"><code><span class=\"line\"><span style=\"color:#E1E4E8\">&#x3C;</span><span style=\"color:#85E89D\">div</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"panel-footer\"</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;</span><span style=\"color:#85E89D\">a</span><span style=\"color:#B392F0\"> href</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"AdministrationHome.aspx\"</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"btn btn-default\"</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">        &#x3C;</span><span style=\"color:#85E89D\">span</span><span style=\"color:#B392F0\"> class</span><span style=\"color:#E1E4E8\">=</span><span style=\"color:#9ECBFF\">\"glyphicon glyphicon-remove\"</span><span style=\"color:#E1E4E8\">>&#x3C;/</span><span style=\"color:#85E89D\">span</span><span style=\"color:#E1E4E8\">></span><span style=\"color:#79B8FF\">&#x26;nbsp;</span><span style=\"color:#E1E4E8\">Cancel</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    &#x3C;/</span><span style=\"color:#85E89D\">a</span><span style=\"color:#E1E4E8\">></span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">&#x3C;/</span><span style=\"color:#85E89D\">div</span><span style=\"color:#E1E4E8\">></span></span></code></pre>\n<p>For forms with both Cancel and Submit (e.g. Admin/AddPIExemption.aspx, Admin/ReportCatalogDetails.aspx): Cancel first, Submit second; Submit often <code>btn btn-primary</code> with <code>pull-right</code> or placed after Cancel.</p>\n<hr>\n<h2 id=\"4-toast-notifications-postback-feedback\">4. Toast notifications (postback feedback)</h2>\n<ul>\n<li><strong>Mechanism</strong>: Session-based. Code-behind adds a <code>Notification</code> to session; master page’s <code>PreRender</code> calls <code>NotificationsSys.DisplayNotifications()</code>; Site.Master JavaScript reads hidden fields and calls <code>showNotification(type, title, text, duration)</code> (jQuery toast plugin).</li>\n<li><strong>Code-behind pattern</strong> (BusinessObjects/Notification.cs, Modules/Utilities.cs constants):</li>\n</ul>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"csharp\"><code><span class=\"line\"><span style=\"color:#E1E4E8\">NotificationsSystem.</span><span style=\"color:#B392F0\">SendNotificationToSession</span><span style=\"color:#E1E4E8\">(</span><span style=\"color:#F97583\">new</span><span style=\"color:#B392F0\"> Notification</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">{</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    NotificationTitle </span><span style=\"color:#F97583\">=</span><span style=\"color:#9ECBFF\"> \"Form Restored\"</span><span style=\"color:#E1E4E8\">,</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    NotificationType </span><span style=\"color:#F97583\">=</span><span style=\"color:#E1E4E8\"> Utilities.NotificationSuccess,  </span><span style=\"color:#6A737D\">// or NotificationError, NotificationWarning, NotificationInfo</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    NotificationText </span><span style=\"color:#F97583\">=</span><span style=\"color:#9ECBFF\"> \"Form restored successfully!\"</span><span style=\"color:#E1E4E8\">,</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">    NotificationDuration </span><span style=\"color:#F97583\">=</span><span style=\"color:#79B8FF\"> 5000</span></span>\n<span class=\"line\"><span style=\"color:#E1E4E8\">});</span></span></code></pre>\n<ul>\n<li><strong>Types and colors</strong> (from Site.Master <code>showNotification()</code>):\n<ul>\n<li><strong>success</strong>: <code>#5cb85c</code></li>\n<li><strong>error</strong>: <code>#d9534f</code></li>\n<li><strong>warning</strong>: <code>#f0ad4e</code></li>\n<li><strong>info</strong>: <code>#5bc0de</code></li>\n</ul>\n</li>\n<li><strong>Durations</strong>: 5000 ms typical; 15000 for long error messages.</li>\n<li>Do not invent new type strings; use <code>Utilities.NotificationSuccess</code>, <code>Utilities.NotificationError</code>, <code>Utilities.NotificationWarning</code>, <code>Utilities.NotificationInfo</code>.</li>\n</ul>\n<hr>\n<h2 id=\"5-inline-alerts-static-or-server-toggled-messages\">5. Inline alerts (static or server-toggled messages)</h2>\n<p>For messages that are part of the page content (not toasts):</p>\n<ul>\n<li><strong>Classes</strong>: <code>alert alert-success</code>, <code>alert alert-info</code>, <code>alert alert-warning</code>, <code>alert alert-danger</code>.</li>\n<li><strong>Thin variant</strong>: <code>alert-thin</code> (customCSSClasses.css) for compact single-line messages.</li>\n<li><strong>Role</strong>: Add <code>role=\"alert\"</code> for accessibility where appropriate.</li>\n<li><strong>Usage</strong>: EmptyDataTemplate, filter info, validation/error blocks (e.g. DeletedDataDashboard EmptyDataTemplate, TrainingMaterials, WorkerForm).</li>\n</ul>\n<hr>\n<h2 id=\"6-data-tables-gridview--datatables\">6. Data tables (GridView + DataTables)</h2>\n<ul>\n<li><strong>GridView</strong>: <code>CssClass=\"table data-display\"</code> and <code>GridLines=\"None\"</code>. Optional: <code>table-hover</code>, <code>table-striped</code>, <code>table-condensed</code> for non–DataTable styling when not initializing with jQuery.</li>\n<li><strong>DataTable initialization</strong> (in <code>ScriptContent</code>): Target <code>.data-display</code>; check grid is visible and has rows; prepend <code>&#x3C;thead></code> from first row if needed; then call <code>.DataTable({ ... })</code>. Example options used in the project:\n<ul>\n<li><code>pageLength: 10</code>, <code>lengthMenu: [10, 25, 50, 100]</code></li>\n<li><code>stateSave: true</code>, <code>stateDuration: 60</code></li>\n<li><code>responsive: { details: { type: 'column' } }</code></li>\n<li><code>language.emptyTable</code>, <code>language.searchPlaceholder</code></li>\n<li><code>columnDefs</code> for non-sortable action column and responsive priority</li>\n</ul>\n</li>\n<li><strong>UpdatePanel</strong>: Re-run the DataTable init in <code>Sys.WebForms.PageRequestManager.getInstance().add_endRequest(...)</code> so it works after partial postbacks.</li>\n<li><strong>Empty state</strong>: Use <code>&#x3C;EmptyDataTemplate></code> with <code>&#x3C;div class=\"alert alert-info\"></code> and clear copy (e.g. “No deleted forms found. Select a form type from the dropdown above.”).</li>\n</ul>\n<hr>\n<h2 id=\"7-modals\">7. Modals</h2>\n<ul>\n<li><strong>Structure</strong>: <code>modal fade</code> → <code>modal-dialog</code> → <code>modal-content</code> → <code>modal-header</code> (with <code>modal-title</code> and close button) → <code>modal-body</code> → <code>modal-footer</code>.</li>\n<li><strong>Buttons</strong>:\n<ul>\n<li>Close/Cancel: <code>class=\"btn btn-default\" data-dismiss=\"modal\"</code> (and optionally glyphicon-remove).</li>\n<li>Confirm/Primary: <code>class=\"btn btn-primary\"</code> (e.g. <code>asp:LinkButton</code> for postback or <code>button</code> for client-only).</li>\n</ul>\n</li>\n<li><strong>Close button in header</strong>: <code>&#x3C;button type=\"button\" class=\"close\" data-dismiss=\"modal\" aria-label=\"Close\">&#x3C;span aria-hidden=\"true\">&#x26;times;&#x3C;/span>&#x3C;/button></code>.</li>\n</ul>\n<hr>\n<h2 id=\"8-colors-and-utility-classes-reference\">8. Colors and utility classes (reference)</h2>\n<ul>\n<li><strong>Toast/notification colors</strong>: See section 4 (success/error/warning/info hex values).</li>\n<li><strong>Custom panel/table</strong> (customCSSClasses): <code>custom-panel-success</code> (#28a745), <code>custom-panel-warning</code> (#e3ae0e), <code>custom-panel-danger</code> (#dc3545); <code>td-warning</code> (#e3ae0e), <code>td-danger</code> (#c42f3e); <code>text-danger</code> (#dc3545); <code>.green</code> / <code>.green-text</code>, <code>.red</code> / <code>.red-text</code>.</li>\n<li><strong>Spacing</strong>: <code>margin-top-10</code>, <code>margin-bottom-10</code>, <code>margin-right-5</code>, <code>margin-left-5</code>, <code>padding-5</code>, etc. (customCSSClasses).</li>\n<li><strong>Form controls</strong>: Use <code>form-control</code> for inputs/dropdowns; <code>form-group</code> for label + control grouping.</li>\n</ul>\n<hr>\n<h2 id=\"9-common-practices-summary\">9. Common practices summary</h2>\n<ul>\n<li><strong>Cancel</strong>: Always provide a way to leave without submitting (link back to parent or Cancel button). Use <code>btn btn-default</code>, left side of footer.</li>\n<li><strong>Submit</strong>: Primary action with <code>btn btn-primary</code>, often with ValidationGroup and OnClick. Place after Cancel; optional <code>pull-right</code>.</li>\n<li><strong>Validation</strong>: Use <code>ValidationGroup</code> on submit buttons and validators; set <code>CausesValidation=\"false\"</code> on Cancel.</li>\n<li><strong>Hidden fields</strong>: Use <code>&#x3C;asp:HiddenField></code> for IDs/keys used by JavaScript and postback (e.g. delete PK, form type).</li>\n<li><strong>Delete in grid</strong>: Use pattern <code>data-pk</code>, <code>data-hf</code> (hidden field ID suffix), <code>data-target</code> (modal selector); master page already wires <code>.delete-gridview</code> to set hidden field and show modal.</li>\n<li><strong>Confirmation for destructive actions</strong>: Use a modal with clear message; Cancel = <code>btn btn-default</code> dismiss; Confirm = <code>btn btn-primary</code> or <code>btn btn-danger</code> as appropriate.</li>\n</ul>\n<hr>\n<h2 id=\"10-design-how-we-design\">10. Design (how we design)</h2>\n<ul>\n<li><strong>Requirements</strong>: Clarify with stakeholders what the form must do (fields, filters, actions, who can see it). Decide if it is a new form, a list/dashboard, or an add/edit detail page.</li>\n<li><strong>Reference existing pages</strong>: Use <strong>DeletedDataDashboard.aspx</strong> for list + filters + grid + modal confirm; use <strong>AdministrationHome.aspx</strong> for landing pages; use <strong>AddPIExemption.aspx</strong> or <strong>ReportCatalogDetails.aspx</strong> for form-with-Submit/Cancel.</li>\n<li><strong>Wireframes/sketches</strong> (optional): For complex flows, sketch layout (filters, table, buttons) so markup and placeholders match.</li>\n<li><strong>Consistency</strong>: Reuse the panel structure, button classes, and patterns in this SOP so the new form looks and behaves like the rest of the app.</li>\n</ul>\n<hr>\n<h2 id=\"11-how-we-get-data\">11. How we get data</h2>\n<p>The app uses two main data patterns (see TechincalDocumentation.md §7):</p>\n<ul>\n<li>\n<p><strong>Entity Framework (newer forms)</strong></p>\n<ul>\n<li><strong>Location</strong>: HFNY/Model/EntityFramework/ — <code>HealthyFamiliesContext</code>, entities (e.g. <code>HVLogDeleted</code>, <code>Worker</code>), and HealthyFamiliesModel.edmx.</li>\n<li><strong>Usage</strong>: In code-behind, create a <code>Model.EntityFramework.HealthyFamiliesContext</code> (or use existing pattern), write LINQ queries, then either bind entities to controls or build a <code>DataTable</code> and bind to a GridView.</li>\n<li><strong>Example</strong>: DeletedDataDashboard.aspx.cs — <code>GetDeletedHVLogs()</code>, <code>GetDeletedSupervisions()</code>, etc. use <code>HealthyFamiliesContext</code> and LINQ, then fill a <code>DataTable</code> and set <code>gvDeletedForms.DataSource = dt; gvDeletedForms.DataBind();</code>.</li>\n</ul>\n</li>\n<li>\n<p><strong>Enterprise Library + stored procedures (legacy)</strong></p>\n<ul>\n<li><strong>Location</strong>: HFNY/BusinessObjects/ — classes call <code>GetStoredProcCommand(\"spName\")</code> and execute via Enterprise Library. Data often maps to BusinessEntities (e.g. <code>beWorker</code>).</li>\n<li><strong>Usage</strong>: When a feature already has a BusinessObject and stored proc, call that from code-behind and bind the returned data to the UI.</li>\n</ul>\n</li>\n<li>\n<p><strong>Typical flow in code-behind</strong></p>\n<ul>\n<li><strong>Page_Load</strong>: If <code>!IsPostBack</code>, call a method that loads data (e.g. <code>LoadAllDeletedForms()</code>) and binds to GridView/DropDownList.</li>\n<li><strong>Events</strong>: Dropdown <code>SelectedIndexChanged</code>, button <code>Click</code>, etc. call server methods that (optionally) reload data and rebind, then show a toast via <code>NotificationsSystem.SendNotificationToSession(...)</code>.</li>\n</ul>\n</li>\n</ul>\n<hr>\n<h2 id=\"12-how-we-test\">12. How we test</h2>\n<ul>\n<li><strong>Manual testing</strong>: Primary approach. Run the app (e.g. F5 or deploy to test env), log in with a role that has access, exercise the form (load, filter, submit, cancel, validation, toasts). Test with different roles if the page is role-restricted.</li>\n<li><strong>Unit tests</strong>: The main project references <code>Microsoft.VisualStudio.QualityTools.UnitTestFramework</code> and has a Testing folder; automated unit tests are not consistently present. New developers can add unit tests for BusinessObjects or utilities if the team adopts that.</li>\n<li><strong>Checklist for a new form</strong>: Can the page be opened? Do filters/grid load? Do Cancel and Submit behave correctly? Do toasts appear after success/error? Does the DataTable (if used) sort/search and re-init after postback? Are validators and ValidationGroup correct?</li>\n</ul>\n<hr>\n<h2 id=\"13-step-by-step-creating-a-new-form\">13. Step-by-step: creating a new form</h2>\n<ol>\n<li>\n<p><strong>Create the page files</strong></p>\n<ul>\n<li>Add <code>YourPage.aspx</code> under the right folder (e.g. <code>HFNY/Admin/</code> or <code>HFNY/Pages/</code>).</li>\n<li>Set <code>MasterPageFile=\"~/MasterPages/Site.Master\"</code> and fill the four content placeholders (HeadContent, FormName, ContentPlaceHolder1, ScriptContent).</li>\n<li>Add <code>YourPage.aspx.cs</code> (and optionally <code>YourPage.aspx.designer.cs</code>). Inherit from <code>System.Web.UI.Page</code> and use the same namespace as the folder (e.g. <code>HFNY.Admin</code>).</li>\n</ul>\n</li>\n<li>\n<p><strong>Markup (ContentPlaceHolder1)</strong></p>\n<ul>\n<li>Wrap content in <code>&#x3C;div class=\"container\"></code>.</li>\n<li>Use the standard panel: <code>panel panel-primary form-panel</code>, with <code>panel-heading</code> (title), <code>panel-body</code> (filters + grid or form fields), and <code>panel-footer</code> (Cancel, then Submit if needed).</li>\n<li>Add controls (GridView with <code>CssClass=\"table data-display\"</code> if using DataTables, DropDownList with <code>CssClass=\"form-control\"</code>, HiddenFields for keys, etc.).</li>\n<li>If you need a confirmation modal, add a <code>modal fade</code> block (see §7).</li>\n</ul>\n</li>\n<li>\n<p><strong>Code-behind</strong></p>\n<ul>\n<li>In <code>Page_Load</code>, if <code>!IsPostBack</code>, call a method to load data and bind (e.g. to GridView or DropDownList).</li>\n<li>Implement event handlers (e.g. <code>ddlFormType_SelectedIndexChanged</code>, <code>btnConfirmRestore_Click</code>). Load/save using EF or BusinessObjects, then rebind and/or <code>SendNotificationToSession</code>.</li>\n<li>Use <code>Utilities.NotificationSuccess</code> / <code>NotificationError</code> etc. and consistent durations (e.g. 5000 ms).</li>\n</ul>\n</li>\n<li>\n<p><strong>JavaScript (ScriptContent)</strong></p>\n<ul>\n<li>If the page has a DataTable, add a function that initializes <code>.data-display</code> (see §6) and call it on <code>jQuery(document).ready</code>. If the page uses UpdatePanel, register the same function in <code>Sys.WebForms.PageRequestManager.getInstance().add_endRequest(...)</code>.</li>\n<li>Add any modal or button handlers (e.g. set hidden fields, show modal) in the same script block.</li>\n</ul>\n</li>\n<li>\n<p><strong>Add to navigation</strong></p>\n<ul>\n<li><strong>Landing page</strong>: If the form is under Settings/Admin, add a link on AdministrationHome.aspx in the right section (e.g. Administration) and RoleGroup: <code>&#x3C;a href=\"../Admin/YourPage.aspx\" data-content=\"Description\">Your Page Title&#x3C;/a></code>.</li>\n<li><strong>Top-level menu</strong>: If it should appear in the main nav, add a node in Web.sitemap with the correct <code>url</code> and <code>roles</code>.</li>\n</ul>\n</li>\n<li>\n<p><strong>Authorization</strong></p>\n<ul>\n<li>Ensure the page is only reachable by allowed roles (same as other Admin pages, or add <code>&#x3C;location></code> in Web.config if needed). AdministrationHome uses <code>asp:LoginView</code> and RoleGroups to show/hide sections.</li>\n</ul>\n</li>\n<li>\n<p><strong>Test</strong></p>\n<ul>\n<li>Follow §12: manual test load, filters, submit, cancel, toasts, and (if applicable) DataTable behavior after postback.</li>\n</ul>\n</li>\n</ol>\n<hr>\n<h2 id=\"14-where-and-how-we-write-javascript\">14. Where and how we write JavaScript</h2>\n<ul>\n<li>\n<p><strong>Page-specific inline (most common)</strong></p>\n<ul>\n<li>Put script in the <strong>ScriptContent</strong> placeholder of the .aspx page. The master page renders this after the bundled scripts (jQuery, Bootstrap, DataTables, etc.).</li>\n<li>Use it for: DataTable initialization, modal open/close, setting HiddenFields from grid buttons, and other logic that only that page needs.</li>\n<li>Example: DeletedDataDashboard.aspx — <code>ScriptContent</code> contains <code>initializeDeletedDataDashboardTable()</code>, click handler for <code>.btnRestore</code>, and <code>document.ready</code> (plus <code>PageRequestManager.add_endRequest</code> for postback).</li>\n</ul>\n</li>\n<li>\n<p><strong>Shared / site-wide</strong></p>\n<ul>\n<li>Scripts used on many pages are in HFNY/Scripts/ and included via App_Start/BundleConfig.cs. The master page loads <code>~/bundles/SiteSpecific</code> (jquery.toast, bootbox, cleave, etc.).</li>\n<li>Global helpers (e.g. <code>showNotification</code>) are defined in Site.Master script block.</li>\n</ul>\n</li>\n<li>\n<p><strong>Page-specific .js file</strong></p>\n<ul>\n<li>For large or reusable page logic, add a new .js file under <code>Scripts/</code> (e.g. <code>YourFeature.js</code>), register a new bundle in BundleConfig (e.g. <code>bundles/YourFeaturePage</code>), and reference that bundle only on the page that needs it (e.g. via ScriptManager or an extra script reference in HeadContent). Many pages instead keep everything in ScriptContent for simplicity.</li>\n</ul>\n</li>\n<li>\n<p><strong>UpdatePanel / partial postback</strong></p>\n<ul>\n<li>After a partial postback, the DOM is replaced; any jQuery plugins (e.g. DataTables) must be re-initialized. Use <code>Sys.WebForms.PageRequestManager.getInstance().add_endRequest(function () { ... })</code> and call the same init function (e.g. <code>initializeDeletedDataDashboardTable()</code>).</li>\n<li>Use event delegation (e.g. <code>jQuery(\".data-display\").on(\"click\", \".btnRestore\", ...)</code>) so that dynamically added rows still work after rebind.</li>\n</ul>\n</li>\n</ul>\n<hr>\n<h2 id=\"15-how-to-create-a-landing-page\">15. How to create a landing page</h2>\n<ul>\n<li><strong>Pattern</strong>: Use <strong>AdministrationHome.aspx</strong> as the template. A landing page is a single page with one or more <strong>sections</strong> (e.g. “Administration”, “Program Management”, “Self-Service”). Each section is a <code>panel panel-primary</code> with <code>panel-heading</code> (title) and <code>panel-body</code> containing <strong>links</strong> to child pages.</li>\n<li><strong>Markup</strong>:\n<ul>\n<li><code>&#x3C;div class=\"panel panel-primary\"></code></li>\n<li><code>&#x3C;div class=\"panel-heading\">&#x3C;h4 class=\"panel-title\">Section Name&#x3C;/h4>&#x3C;/div></code></li>\n<li><code>&#x3C;div class=\"panel-body\"></code></li>\n<li>Multiple <code>&#x3C;a href=\".../ChildPage.aspx\" data-content=\"Tooltip\">Display Text&#x3C;/a></code></li>\n<li>Optional: add <code>list-group-item</code> class to links (AdministrationHome does this via jQuery).</li>\n</ul>\n</li>\n<li><strong>Role-based visibility</strong>: Use <code>&#x3C;asp:LoginView></code> and <code>&#x3C;RoleGroups></code> so only certain roles see certain sections (e.g. Admin sees “User Administration”, “Restore Deleted Forms”, etc.). Add new links inside the appropriate RoleGroup.</li>\n<li><strong>Adding a new child link</strong>: Find the right panel and RoleGroup on AdministrationHome (or your landing page), then add a line: <code>&#x3C;a href=\"../Admin/YourNewPage.aspx\" data-content=\"Short description\">Your New Page&#x3C;/a></code>. No Web.sitemap change needed unless the page should also appear in the top-level menu.</li>\n</ul>\n<hr>\n<h2 id=\"16-database-setup-ssms-and-frontend-use\">16. Database setup (SSMS and frontend use)</h2>\n<p>When a new form needs <strong>new or changed database objects</strong>:</p>\n<ul>\n<li>\n<p><strong>Connection configuration</strong></p>\n<ul>\n<li>Connection strings live in <strong>connections.config</strong> (or environment-specific config). The default database is typically named <strong>HFNY</strong> (see TechincalDocumentation §4–5). Do not commit real connection strings; use config transforms or externalized config per environment.</li>\n</ul>\n</li>\n<li>\n<p><strong>Option A: New or changed tables (Entity Framework)</strong></p>\n<ul>\n<li>Create or alter tables in <strong>SQL Server Management Studio (SSMS)</strong> (or run a .sql script in the project/repo if the team uses one).</li>\n<li>In Visual Studio, update the <strong>EF model</strong>: open HFNY/Model/EntityFramework/HealthyFamiliesModel.edmx, right-click → “Update Model from Database”, and add/refresh the tables. This regenerates entity classes under Model/EntityFramework.</li>\n<li>In code-behind (or a BusinessObject), use <code>HealthyFamiliesContext</code> and the new entities/DbSets to query and persist. No stored proc required for simple CRUD if using EF.</li>\n</ul>\n</li>\n<li>\n<p><strong>Option B: Stored procedures (Enterprise Library)</strong></p>\n<ul>\n<li>Create or alter stored procedures in SSMS (or via a .sql script).</li>\n<li>In BusinessObjects (or rarely in code-behind), call <code>db.GetStoredProcCommand(\"YourSpName\")</code>, add parameters, execute, and map results to entities or a DataTable.</li>\n<li>Use this when the feature already follows the BusinessObject + stored-proc pattern or when complex logic is kept in the database.</li>\n</ul>\n</li>\n<li>\n<p><strong>Summary for new developers</strong></p>\n<ul>\n<li><strong>New table, simple CRUD</strong>: Create table in SSMS → Update EF model from database → use <code>HealthyFamiliesContext</code> and LINQ in the new page.</li>\n<li><strong>New stored proc</strong>: Create proc in SSMS → call it from a BusinessObject or code-behind via Enterprise Library.</li>\n<li><strong>Existing tables</strong>: Use existing EF entities or BusinessObjects; no SSMS change unless you add columns or procs.</li>\n</ul>\n</li>\n</ul>",{headings:95,localImagePaths:149,remoteImagePaths:150,frontmatter:151,imagePaths:154},[96,98,101,104,107,110,113,116,119,122,125,128,131,134,137,140,143,146],{depth:32,slug:97,text:83},"form-development-sop-for-new-developers",{depth:56,slug:99,text:100},"goal","Goal",{depth:56,slug:102,text:103},"1-page-structure-and-master-page","1. Page structure and master page",{depth:56,slug:105,text:106},"2-form-panel-layout-standard-form-look","2. Form panel layout (standard “form” look)",{depth:56,slug:108,text:109},"3-buttons--css-classes-and-placement","3. Buttons – CSS classes and placement",{depth:56,slug:111,text:112},"4-toast-notifications-postback-feedback","4. Toast notifications (postback feedback)",{depth:56,slug:114,text:115},"5-inline-alerts-static-or-server-toggled-messages","5. Inline alerts (static or server-toggled messages)",{depth:56,slug:117,text:118},"6-data-tables-gridview--datatables","6. Data tables (GridView + DataTables)",{depth:56,slug:120,text:121},"7-modals","7. Modals",{depth:56,slug:123,text:124},"8-colors-and-utility-classes-reference","8. Colors and utility classes (reference)",{depth:56,slug:126,text:127},"9-common-practices-summary","9. Common practices summary",{depth:56,slug:129,text:130},"10-design-how-we-design","10. Design (how we design)",{depth:56,slug:132,text:133},"11-how-we-get-data","11. How we get data",{depth:56,slug:135,text:136},"12-how-we-test","12. How we test",{depth:56,slug:138,text:139},"13-step-by-step-creating-a-new-form","13. Step-by-step: creating a new form",{depth:56,slug:141,text:142},"14-where-and-how-we-write-javascript","14. Where and how we write JavaScript",{depth:56,slug:144,text:145},"15-how-to-create-a-landing-page","15. How to create a landing page",{depth:56,slug:147,text:148},"16-database-setup-ssms-and-frontend-use","16. Database setup (SSMS and frontend use)",[],[],{title:83,author:85,pubDate:152,description:84,codeLocation:86,categories:153},["Date","2025-02-11T00:00:00.000Z"],[20],[]];

export { _astro_dataLayerContent as default };
